<view 
  class="pickerTips"
  onTap="openPicker">当前选择的元素：
  <picker cols="{{3}}"
    className="{{className}}"
    disabled="{{false}}"
    onFormat="onFormat"
    onDismiss="onDismiss"
    onOk="onOk"
    value="{{value}}"
    onChange="onChange"
    pickerholder="请选择"
    data="{{pickerList}}">
    <view slot="title">Picker选择器</view>
  </picker>
</view>